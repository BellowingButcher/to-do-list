import React from 'react';
import ReactDOM from 'react-dom/client';
import TaskInputSB from './sandbox/TaskInputSB';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
  <TaskInputSB />
  </React.StrictMode>
);
